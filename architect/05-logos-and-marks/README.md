# Logos and marks

Drop logo files here.

Useful files:

- Primary logo (colour, black, white)
- Wordmark and symbol / icon only
- App icon and favicon
- Social avatars
- SVG first, then PNG at 1x / 2x if SVG is not available

Prefer vector (`.svg` or `.ai` / `.eps` with an SVG export).
